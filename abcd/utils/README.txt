By default, get_abcd_data.py script looks for ABCD data at a local path relative to the repo.
You can override this by setting an environment variable for the location of your abcd dataset:

export ABCD_DATA_DIR="/path/to/your/data"